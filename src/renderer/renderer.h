#ifndef RENDERER_RENDERER_H
#define RENDERER_RENDERER_H

namespace renderer {
  /// @brief Initialize some OpenGL stuff
  /// @returns Whether initialization was successful
  bool init();
} // namespace renderer

#endif // RENDERER_RENDERER_H
